<% @user.name %>
<div class="row">
	<aside class="col-md-4">
		<section class="user_info">
			<h1>
			<%= gravatar_for @user %>
			<%= @user.name %>
			</h1>
		</section>
	</aside>
	<section class="status_following">
		<%= render 'shared/status_following'%>
		<% if @pages.any? %>
			<% @pages.each do |page| %>
			<%= link_to  page %>
			<% if 1  %>
		
					| <%= link_to "prestani pratiti", @page.passive_relationships.find_by(follower_id: user.id,followed_id: @page.id), method: :delete,data: { confirm: "Jeste li sigurni?" } %>
				<% end %>	
			<% end %>
			</div>
		<% end %>
	</section>
	<div class="col-md-8">
		<h3><%= @title %></h3>
	</div>
</div>